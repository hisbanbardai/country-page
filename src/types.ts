export type TCountry = {
  flags: {
    png: string;
  };
  name: {
    common: string;
  };
  population: string;
  area: string;
  region: string;
  independent: boolean;
  cioc: string;
  subregion: string;
};
